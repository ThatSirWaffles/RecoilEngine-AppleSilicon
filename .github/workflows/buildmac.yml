name: Build macOS App

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          lfs: true
<<<<<<< HEAD
=======

      - name: Check required files
        run: |
          git submodule status --recursive
          ls -la testkit/
          ls -la testkit/libm-refs/
>>>>>>> f9e5528fd106f95559c05e10055068e183483cb2

      - name: Build app
        run: make app

      - name: Upload app
        uses: actions/upload-artifact@v4
        with:
          name: bar-launcher-macos
          path: |
            **/*.dmg
